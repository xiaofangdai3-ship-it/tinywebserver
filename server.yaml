# TinyWebServer 配置文件
# 键值对格式：key: value

# 监听端口
port: 8080

# 线程池大小（工作线程数）
threads: 8

# 连接超时时间（毫秒）
connection_timeout: 60000

# 日志级别：0=DEBUG, 1=INFO, 2=WARN, 3=ERROR
log_level: 1

# 日志文件路径（空字符串表示只输出到控制台）
log_file: ""

# 是否输出日志到控制台
console_log: true
